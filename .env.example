# Configuración de Base de Datos PostgreSQL
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=inventory_db
DB_PORT=5432

# Configuración del Servidor
PORT=3001
NODE_ENV=development

# Configuración de PostgreSQL para pgAdmin
# Asegúrate de que PostgreSQL esté corriendo en el puerto 5432
# Usuario: postgres (o el usuario que uses en PostgreSQL)
# Contraseña: postgres (o la contraseña que hayas configurado)
# Base de datos: inventory_db (se creará automáticamente si no existe)" 